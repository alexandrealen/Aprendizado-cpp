/*
#include <cctype>
isalpha (é um caráter alfabético)
isdigit (é a representação de um dígito)
isspace (é espaço em branco)
toupper (converter para maiúscula)
tolower (converter para minúscula)

...
char ch;
...
if (isalpha(ch)) ...
*/

/*
#include <cmath>
pow(base, expoente)
sqrt(numero)
ceil(numero) -- arredonda o numero pra cima
floor(numero) -- arredonda o numero pra baixo
*/

/*
#include <string.h>
char texto[100];
strlen(texto) -- numero de caracteres no texto
*/

/*
cout.precision(2);
cout.setf(ios::fixed);
*/
